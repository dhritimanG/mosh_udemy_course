/**
 * Underscore provides over 100 functions that support 
 * both your favorite workaday functional helpers: map, filter, invoke
 * — as well as more specialized goodies: function binding, javascript templating, 
 * creating quick indexes, deep equality testing, and so on.
 */

var _ = require('underscore');  
var result = _.contains([1,2,3], 3);

console.log(result);